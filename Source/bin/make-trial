#!/bin/bash
cd "`dirname "$0"`" ; cd ..
cp cobra.exe cobra-trial.exe
if [ -f cobra.exe.mdb ]; then
	cp cobra.exe.mdb cobra-trial.exe.mdb
fi
echo 'You can now run build-with-trial to compile the compiler source code with a trial snapshot of the new compiler.'
