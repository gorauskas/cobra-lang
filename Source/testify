clean() {
	cd ../Tests/
	./clean
	cd ../Source
}

clear

clean

# set mono path to support using the Cobra.Lang.dll in this directory
export MONO_PATH="`pwd`:$MONO_PATH"

if [ -f r-run ]; then
	mv r-run r-run-last
fi
cobra -testify $*
edit r-run

clean
