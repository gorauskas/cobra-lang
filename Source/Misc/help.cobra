use System.Reflection

class X

	get two as int
		return 2

	def main
		s = 'aoeu'
		len = @help s.length
		@help s.replace('x', 'y')
		@help assert len > 0
		@help s
		t = @help ['foo', 'bar']
		a = @help Assembly.getEntryAssembly
		x = @help X()
		@help X
		s.@help
		.@help
		trace len, t, a, x
