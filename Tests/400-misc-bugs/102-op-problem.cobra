# former false parse error
class Test

	def main is shared
		pass

	var _stuff as Object?

	get stuff from var

	def foo
		x = this
		if x.stuff is nil
			_stuff = Object()
		if x.stuff is not nil
			_stuff = Object()
