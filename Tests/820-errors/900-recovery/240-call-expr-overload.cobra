class X

	def main is shared
		.foo(.bar)  # .error. For "foo" arg 1: Cannot access instance member "bar" from the current shared member.

	def foo(i as int) is shared
		pass

	def foo(s as String) is shared
		pass

	def bar as int
		return 1
