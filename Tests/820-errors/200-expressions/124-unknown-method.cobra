class Test

	def main
		.badName(2)  # .error. not

		_baz(0).trim  # .error. Cannot find a definition for "_baz" in "this" whose type is "Test".
		
		# test a method name with correct spelling, wrong case, against a method overload
		StringWriter().writeline('')  # .error. Cannot find a definition for "writeline" in "StringWriter()" whose type is "StringWriter".
