class Test

	def main
		is shared
		pass

	def foo
		b as int = bar  # .error. leading dot
		CobraCore.noOp(b)

	def bar as int
		return 0

	def baz1(i as int) as int
		return 0

	def baz2(i as int) as int
		return baz1(i)  # .error. You must refer to non-underscored members with a leading dot (.). Member name is "baz1".

	def baz3(i as int) as int
		return baz(i)  # .error. Cannot find "baz". There are members with similar names including "baz1", "baz2" and "baz3" if you were thinking of member access
