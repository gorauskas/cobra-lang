struct N

	cue init(value as int)
		base.init
		_value = value
		
	get value from var as int


class P

	def main is shared
		n = N(5)
		if n     # .error. Cannot determine truth from an arbitrary struct.
			print 'n is true'
		while n  # .error. Cannot determine truth from an arbitrary struct.
			print 'n is true'
