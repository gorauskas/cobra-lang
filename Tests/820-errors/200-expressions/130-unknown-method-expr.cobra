# .error. Cannot find a definition for "existss" in "Path"

# used to say: Cannot find a definition for "existss" in Type.
# which was confusing

class Foo

	def main is shared
		if Path.existss('foo')
			print 'foo'
