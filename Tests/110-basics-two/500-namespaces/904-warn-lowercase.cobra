class X
	pass

namespace foo  # .warning. Namespace names should start with an uppercase letter in order to avoid collisions with other identifiers such as arguments and local variables.

	class P
	
		def main is shared
			print 'hello'
