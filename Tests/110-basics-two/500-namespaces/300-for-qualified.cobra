class Test

	def main is shared
		Test().run

	def run
		.check(CobraCore.commandLineArgs)
		.check(CobraCore.commandLineArgs)

	def check(stuff as System.Collections.IEnumerable)
		for item in stuff	
			assert item
