# use names for parameters and locals that are keywords in C#

class Test

	shared

		def main
			.check(5, 5)
		
		def check(object as Object, i as int)
			assert object == i

		def foo(case as bool)
			assert case
