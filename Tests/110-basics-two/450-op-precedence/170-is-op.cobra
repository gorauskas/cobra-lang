# This isn't really about precendence as the containing directory would indicate

# It's about the `is` operator. We need an `operators` directory in the test suite

class Test

	def main is shared
		x = 5.1
		assert x.getType is decimal
