class X

	def main is shared
		x = 5
		y = 10

		if x < y _
		and x < 100
			assert x

		if x < y _
			and x < 100
			assert x

		if x < y _
				and x < 100
			assert x

		if x < y _
			and x < 100

			assert x

		if x < y _
			and x < 100
			# foo
			assert x

		if x < y _
		   and x < 100
			assert x

		if x < y _
		          and x < 100
			assert x

		CobraCore.noOp(x _
			, y)


class _
	Y
	pass


class _
Z
	pass
