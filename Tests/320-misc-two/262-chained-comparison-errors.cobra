class Test
	
	enum MyEnum
		burrito
		carne
		chili
		enchilada
		flan
		taco
	
	def main
		result = 4 > MyEnum.carne == 'hi' # .error. cannot be compared
		result = 'hi' > 4 <> MyEnum.carne # .error. cannot be compared
		result = MyEnum.carne > 'hi' <> 4 # .error. cannot be compared
		print result
		