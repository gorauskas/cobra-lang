# used to be a bug with the parser+tokenizer
class Test

	def doSomething(a as Object, b as Object)
		pass

	def run
		args = [1, 2]
		.doSomething(args[0], args[1])

	def main
		is shared
		t = Test()
		t.run
