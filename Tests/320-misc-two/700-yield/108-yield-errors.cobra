# .error. Cannot return int & Cannot return a value from an iterator. & Cannot return int & Cannot return a value from an iterator.

class Test

	def main is shared
		pass
		
	def foo as IEnumerable<of int>
		return 5
		yield 1
	
	def bar as IEnumerable<of int>
		yield 1
		return 5
