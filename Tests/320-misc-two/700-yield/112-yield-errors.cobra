# .error. Cannot return decimal because "foo" is declared to return a int. & Cannot yield unless the return type is an iterator type (IEnumerable or IEnumerator, generic or not). Try

class Test

	def main is shared
		pass
		
	def foo as IEnumerable<of int>
		yield 1.1

	def bar as List<of int>
		yield 1
