# .args. -embed-run-time:no -ref:400-foolib

# same as other fooprog, but the -r arg does not include the ".dll" extension -- both ways should work

class Program

	def main is shared
		f = Foo()
		print f.one
		print f.two
