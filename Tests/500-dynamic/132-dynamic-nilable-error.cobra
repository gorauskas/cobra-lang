class Test

	def main is shared
		x as dynamic?

		.takeDynamic(x)  # .error. Argument 1 of method "takeDynamic" expects a non-nilable type (dynamic), but the call is supplying a nilable type (dynamic?).

	def takeDynamic(x as dynamic) is shared
		pass
