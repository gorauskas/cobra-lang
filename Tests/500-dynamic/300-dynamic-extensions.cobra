# Show that the extension method IList.swap found in the Cobra standard library is reachable via dynamic binding.

# TODO: More work is needed in this area. Check for HACK and TODO in CobraLang.cs


class Program

	def main is shared
		t = [1, 2, 3]
		d = t to dynamic
		d.swap(1, 2)
