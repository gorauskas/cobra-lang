class Test

	def main is shared
		
		s1 as String = nil  # .error. cannot assign

		s2 as String = 'aoeu'
		s2 = nil  # .error. cannot assign
		
		s3 as String  # .error. must initialize
		s3 = 'aoeu'

		CobraCore.noOp(s1, s2, s3)
