class Test

	shared
		def main
			s as String?
			assert s is nil
			assert s==nil
			s = nil
			s = 'aoeu'
			s = nil

			# methods and properties can be invoked on nilable, so slicing can be too
			s = 'aoeu'
			assert s[:2] == 'ao'
