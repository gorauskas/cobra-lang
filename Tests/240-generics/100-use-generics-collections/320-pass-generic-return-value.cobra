class Program

	def run
		args = CobraCore.commandLineArgs
		if args.count>1
			.handle(args.getRange(1, args.count-1))

	def handle(paths as List<of String>)
		print paths

	def main is shared
		pass
