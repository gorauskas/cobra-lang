class Test

	# This syntax is provided because it matches what methods often use, but
	# 206-fields.cobra shows the more typical syntax.
	var _msg as String
		is shared
		
	def main
		is shared
		_msg = 'a'
		assert _msg=='a'
