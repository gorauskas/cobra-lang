# comment
"""
Top of the module doc string.
"""

class Test

	enum Color
		"""
		Enum docs.
		""" 
		# there is a trailing space on the above line (or at least there should be--this used to reveal a bug in the parser)
		Red
		Green
		Blue

	def main
		is shared
		pass
