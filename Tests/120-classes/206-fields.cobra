class Test

	# This is the typical syntax for declaring shared variables.
	# You can also put methods and properties inside the shared block if you wish.
	shared
		var _msg as String
		var _count as int
		
		def main
			_msg = 'a'
			assert _msg=='a'
			_count = 1
			assert _count==1

	var _foo as int
