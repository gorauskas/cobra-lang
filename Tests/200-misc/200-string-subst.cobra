# a few different elements are needed for this test
class Test

	shared
		def main
			Test().run

	def run
		assert '[.foo][.bar]'=='nilnil'

	get foo as String
		return nil to passthrough

	get bar as String
		return nil to passthrough
