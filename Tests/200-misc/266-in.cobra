sig FilterType(t as Type) as bool

class TestProgram

	def main
		implicitNamespaces = @['a', 'b']	
		isImplicit as FilterType = do(someType as Type)
			return someType.isPublic and someType.namespace in implicitNamespaces
		CobraCore.noOp(isImplicit)