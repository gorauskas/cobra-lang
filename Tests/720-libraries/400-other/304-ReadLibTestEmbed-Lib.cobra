# .compile-only.
# .args. -c -embed-run-time:yes -t:lib -out:ReadLibTestLibEmbed.dll

namespace ReadLibTestLibEmbed

	enum LibEnum
		of uint64
		A
		B

	class LibClass
	
		def takeNonNilString(s as String)
			pass