# assignment can be used in an expression
namespace Test

	class Test
	
		def main
			is shared
			x as int = 0
			y as int = x = 1
			a as int = b as int = 5
			assert a==5
			assert b==5
			assert y == 1
			CobraCore.noOp(x)
