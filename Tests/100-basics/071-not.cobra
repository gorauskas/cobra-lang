namespace Test

	class Test

		def main
			is shared

			assert not false
			assert not 0
			assert not 0.0
			assert not 0.0f
			assert not nil  # .warning. always

			x as int = 5
			assert not x==1
