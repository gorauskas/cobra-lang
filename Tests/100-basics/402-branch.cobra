class Test

	def main is shared

		word as String = ''

		x as int = 2
		branch x
			on 1
				word = 'one'
			on 2
				word = 'two'
			on 3
				word = 'three'
			if x  # .error. expecting
				word = 'woops'
