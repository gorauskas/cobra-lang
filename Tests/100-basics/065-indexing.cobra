namespace Test
	
	class Test
		
		def main
			is shared

			assert Environment.getCommandLineArgs[0].length
			assert Environment.getCommandLineArgs[0]<>'roembrjkxte,.nune,.ntokmbk'
			s as String = Environment.getCommandLineArgs[0]
			t as String = Environment.getCommandLineArgs[0]
			s = t
			assert s==t
			assert t==Environment.getCommandLineArgs[0]
