# test a list of types
# test a list of classes

class X
	pass

class Y
	pass

enum E
	A
	B

class Program

	def main is shared
		classes = [X, Y, E]
		for c in classes
			obj = c()
			assert obj is not nil
