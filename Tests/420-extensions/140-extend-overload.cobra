extend String

	def split(i as int) as String[]?
		return String[](0)

	def split(d as decimal) as String[]?
		return String[](0)


class X

	def main is shared
		s = 'aoeu'
		assert s.split(5).length == 0
		assert s.split(5.1).length == 0
